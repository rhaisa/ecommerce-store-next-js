import Link from 'next/link';

const AddToBasketButton = props => {
  return (
    <React.Fragment>
      <button className="button btn btn-outline-dark">Add to basket</button>
    </React.Fragment>
  );
};

export default AddToBasketButton;
